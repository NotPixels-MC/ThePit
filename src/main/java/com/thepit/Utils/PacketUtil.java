package com.thepit.Utils;

import org.bukkit.entity.Player;

public class PacketUtil {

    public static void sendPlayerInfoAdd(Player viewer, Bots bot) {
        // Build and send PlayerInfo ADD packet
    }

    public static void sendPlayerInfoRemove(Player viewer, Bots bot) {
        // Build and send PlayerInfo REMOVE packet
    }

    public static void sendSpawn(Player viewer, Bots bot) {
        // Build and send NamedEntitySpawn packet
    }

    public static void sendDestroy(Player viewer, Bots bot) {
        // Build and send EntityDestroy packet
    }

    public static void sendTeleport(Player viewer, Bots bot) {
        // Build and send EntityTeleport packet
    }

    public static void sendMetadata(Player viewer, Bots bot) {
        // Build and send metadata packet
    }

    public static void sendHurtAnimation(Player viewer, Bots bot) {
        // Build and send EntityStatus packet
    }
}